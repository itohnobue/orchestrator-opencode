=== TASK (s0-planner) ===
PROJECT: [fill in project path]
YOUR TASK: Research this project thoroughly and produce a custom Orchestration Workflow manifest. Explore the full codebase structure, read the agent INDEX at `.opencode/agents/INDEX.md`, classify the task on 5 axes (size, domain breadth, ambiguity, severity, change type), select appropriate bricks from the palette, and write the plan to `tmp/glm-plan.md`.
MUST ANSWER:
1. How many distinct domain areas does this project have? Map each to the best specialist agent from the INDEX.
2. What is the task classification on all 5 axes, with code-context justification for each?
3. Which bricks are selected and why? Which are skipped and why? Include CONVERGE variant justification (NONE/ONCE/LOOP) — base the decision on ambiguity, codebase complexity, severity, and production impact.
3b. For every DISCOVER and REVIEW stage (including CONVERGE iterations of these stages): state whether second opinions are required based on severity (MEDIUM+ → mandatory). For each stage that requires a second opinion, explicitly name the second agent in the stage's plan entry — the lead spawns whatever agents the plan lists, so it must be in the plan. Use a different agent .md from the primary (see AGENTS.md Second Opinion Guidelines for default pairings). If no second opinion is required (severity < MEDIUM or LOW-only findings), justify briefly.
4. Should the plan include a RESEARCH stage? The planner should default to adding RESEARCH when the task touches anything outside the codebase (external standards, compliance requirements, unfamiliar technology, authoritative references). Skip RESEARCH only when the task is purely internal — all needed knowledge is within the codebase. If adding RESEARCH, describe the research question(s), propose the RESEARCH variant (SINGLE/MULTI), and name the research agent(s) from the INDEX. If skipping, justify why the task is purely internal.
5. For each agent you plan in the DISCOVER stage, state the agent's FILE SCOPES — module-level descriptions of what the agent should audit (e.g., "GPG core: core/GPGHandler.py, core/gpg_utils/*.py"). Include a rough LOC estimate from Phase 1 for each scope. Do NOT list individual file paths — the organizer resolves scopes to exact KEY FILES. Keep each agent to ~20 files / ~5K LOC estimated; narrow overages (up to ~25 files / ~6K LOC) are acceptable only when the same cohesive module requires reading all files together. Split by module or concern for anything over those estimates. If the task spans 2+ domains using DIFFERENT specialists: (a) classify each domain boundary using the Boundary Selection tiers (ALWAYS/DEFAULT/SKIP), (b) add intersection agents to DISCOVER for ALWAYS/DEFAULT boundaries — intersection agents MUST be in the first DISCOVER stage, never deferred to CONVERGE, (c) add cross-domain integration reviewers to REVIEW for ALWAYS/DEFAULT boundaries, (d) document all boundary decisions in the manifest under "Boundary Analysis."
6. For each domain and sub-group agent, provide FILE SCOPES (module-level descriptions of what the agent covers — not individual file paths; e.g., "Mail sync module: core/sync.py, core/mail/*.py"). Include rough LOC estimates from Phase 1 research. Do NOT try to list exact file paths — the organizer owns path resolution and exact LOC counts. Include 3-6 MUST ANSWER questions with expected evidence types (based on your Phase 1 code research). Include second opinion scope.
7. What is the total agent estimate, broken down by stage?
8. Do the build and test commands work? Run them and report results. If the project's AGENTS.md or README says not to run them locally, state that and skip. Include the verified working commands (or reason for skipping) in the plan.

STOP AFTER WRITING THE PLAN. When `tmp/glm-plan.md` is written, your work is complete. Do NOT execute any stage, spawn any agents, prepare any task files for Stage 1+, copy any files, or run any verification. The plan IS your deliverable. The lead handles all execution.

=== TASK (s0-organize) ===
PROJECT: [same project path]
PRIOR CONTEXT: The plan is at `tmp/glm-plan.md`.
YOUR TASK: Read the plan at `tmp/glm-plan.md`. Review it using your dual analytical framework (see agent-organizer.md):

0. FILE SCOPE RESOLUTION (do this FIRST, before structural checks). For every domain agent that has FILE SCOPES (not exact KEY FILES), resolve each scope to exact individual file paths. For each scope entry (e.g., "core/GPGHandler.py, core/gpg_utils/*.py"), run `glob` on the wildcard patterns, `find` for module directories, and `test -f` on explicit files to expand into the full list of actual files. Write the resolved KEY FILES into the plan, replacing the planner's FILE SCOPES but PRESERVING the planner's MUST ANSWER questions, domain descriptions, and agent assignments for each domain. Then run `wc -l` on every resolved file. Compare the actual LOC against the planner's rough LOC estimate. If the actual LOC exceeds the 25 file / 6K LOC narrow cap, split the domain agent — add a new domain agent in the plan with its own scope, agent assignment, and second opinion. After splitting, re-evaluate: if any resulting sub-agent has fewer than 15 files AND fewer than 3K LOC, the split created an under-utilized agent. Stand-alone agents this small add coordination overhead without proportional audit depth. Consider merging adjacent sub-agents back — the combined domain may fit the narrow cap or be a better close call than a set of fragmented agents. A 40-file domain at 4K LOC split into two 20f/2K-LOC agents is fragmentation; one 40f/4K-LOC agent with "many thin boilerplate files" justification is better. If the actual LOC is above the ~20 file / ~5K baseline but under the narrow cap, flag it as a close call with justification. When file count exceeds the 25f cap but total LOC is under 3K, the files are likely thin (short code-behind, small utilities) — prefer accepting as close call over splitting. Replace the planner's rough estimates with exact file counts and LOC in the plan. Produce a systematic volume audit table showing every domain: actual files, actual LOC, baseline check, narrow-cap check, and verdict (pass / close call / split). When you adjust a domain's scope (splitting, narrowing, redistributing files between agents), also update the domain's KEY FILES column in the plan's inline table — move file entries to the new agent, remove them from the old agent. The volume audit table and the inline KEY FILES must agree.
1. Workflow quality (native anti-patterns): Check for over-staffing, wrong agent assignments, redundant agents, vague delegations, ignored dependencies, and stale agent references.
2. Structural validation: Verify every DISCOVER/REVIEW stage has a corresponding VERIFY. Verify IMPLEMENT stages have a corresponding REVIEW. Verify MEDIUM+ severity tasks have second opinions in ALL DISCOVER and REVIEW stages, including CONVERGE iterations. This includes intersection agents — at MEDIUM+ severity, each intersection agent gets a second opinion with a different specialist. If the plan has intersection agents without corresponding second opinions, flag and add them. Verify FIX stages include post-fix REVIEW. Verify CONVERGE variant matches the codebase characteristics documented in the plan's own research — specifically: if the plan's research shows comprehensive test coverage (>80%) and clean module boundaries, CONVERGE=ONCE requires stronger justification than "medium ambiguity" (ONCE is for interconnected modules, dense coupling, non-uniform patterns, >15K LOC per domain, or HIGH+ severity, not a universal default). If CONVERGE iteration 2 is planned, verify no agent is reused from iteration 1 — different iterations should use genuinely different specialists. Swapping primary↔second-opinion roles between iterations does NOT satisfy this — the same pair of agents is still the same analytical framework. If the plan has an exclusion list, cross-check EVERY iter 2 primary, second opinion, AND intersection agent against it mechanically — do NOT rely on the plan's claim that agents differ. If any excluded agent appears in iter 2, replace it. Verify cross-domain integration review only runs when genuinely different specialists are at integration boundaries. Verify domain breadth counts specialists, not packages. Volume splitting and close-call flagging are handled in Step 0 (scope resolution + systematic volume audit table) — do NOT duplicate them here. Verify sequential stages are genuinely dependent — if stage N+1 reads different files than stage N and does not consume stage N's verified output, flag for merge into a single parallel stage. When the plan spans 2+ domains: verify a Boundary Analysis section exists, each boundary is triaged (ALWAYS/DEFAULT/SKIP), ALWAYS/DEFAULT boundaries have intersection agents in DISCOVER and cross-domain reviewers in REVIEW, and SKIP boundaries have one-line justification. Flag missing triage or unjustified skips. Verify VERIFY stages separate CONVERGE iterations — the plan must have a VERIFY stage between iter 1 and iter 2 (not a single VERIFY after both). Merged verification means iter 2 has no PRIOR CONTEXT and no decision gate.

3. Mechanical plan correctness: verify every KEY FILE you resolved in Step 0 exists on disk. Use `test -f` on each resolved path. If any file does not exist at its resolved path, search for it with `find` and correct it. Ensure no stale planner-supplied path entries remain in the plan — every file listed must be scope-resolved and verified by you.

After review, apply all fixes directly to `tmp/glm-plan.md`. Your report documents what was changed and why. Your output IS the final plan — no separate merge agent is needed. This review runs on EVERY plan, not just CRITICAL severity.
WRITABLE FILES: tmp/glm-plan.md
MUST ANSWER:
1. What issues were found in the plan? (brick selection, severity classification, agent assignments, unresolvable FILE SCOPES, missing MUST ANSWER questions, dependencies, structural validation gaps, volume splitting omissions, CONVERGE justification gaps)
2. What changes were applied to produce the final plan? List each change with the specific issue it fixed.
3. Were any review findings rejected, and why?
4. Does the final plan have correct brick selection, severity classification, structural validation, mechanically-verified existing KEY FILES for all agents, and valid MUST ANSWER questions?
