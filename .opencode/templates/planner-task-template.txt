=== TASK (s0-planner) ===
PROJECT: [fill in project path]
YOUR TASK: Research this project thoroughly and produce a custom Orchestration Workflow manifest. Explore the full codebase structure, read the agent INDEX at `.opencode/agents/INDEX.md`, classify the task on 5 axes (size, domain breadth, ambiguity, severity, change type), select appropriate bricks from the palette, and write the plan to `tmp/glm-plan.md`.
MUST ANSWER:
1. How many distinct domain areas does this project have? Map each to the best specialist agent from the INDEX.
2. What is the task classification on all 5 axes, with code-context justification for each?
3. Which bricks are selected and why? Which are skipped and why? Include CONVERGE variant justification (NONE/ONCE/LOOP) — base the decision on ambiguity, codebase complexity, severity, and production impact.
4. Which specialized agents from the INDEX fit each domain? Does any specialist domain exceed a single agent's context window (~50-100 files / 15-25K LOC)? If so, apply Step 2 (split by volume): divide into sub-groups by module or concern, keeping each under the context limit. State the per-sub-group file count and LOC. If the task spans 2+ domains using DIFFERENT specialists, add a cross-domain integration reviewer to the REVIEW stage.
5. For each domain and sub-group agent, provide specific KEY FILES (file paths with line counts) and 3-6 MUST ANSWER questions with expected evidence types. Include second opinion scope.
6. What is the total agent estimate, broken down by stage?
7. Do the build and test commands work? Run them and report results. If the project's AGENTS.md or README says not to run them locally, state that and skip. Include the verified working commands (or reason for skipping) in the plan.

=== TASK (s0-organize) ===
PROJECT: [same project path]
PRIOR CONTEXT: The plan is at `tmp/glm-plan.md`.
YOUR TASK: Read the plan at `tmp/glm-plan.md`. Review it using your dual analytical framework (see agent-organizer.md):
1. Workflow quality (native anti-patterns): Check for over-staffing, wrong agent assignments, redundant agents, vague delegations, ignored dependencies, and stale agent references.
2. Structural validation: Verify every DISCOVER/REVIEW stage has a corresponding VERIFY. Verify IMPLEMENT stages have a corresponding REVIEW. Verify MEDIUM+ severity tasks have second opinions in DISCOVER and REVIEW. Verify FIX stages include post-fix REVIEW. Verify cross-domain integration review only runs when genuinely different specialists are at integration boundaries. Verify domain breadth counts specialists, not packages. Verify volume splitting: if any domain exceeds ~50 files / 15K LOC, the planner must split it into sub-groups — check that the plan has sufficient agents per volume tier.

After review, apply all fixes directly to `tmp/glm-plan.md`. Your report documents what was changed and why. Your output IS the final plan — no separate merge agent is needed. This review runs on EVERY plan, not just CRITICAL severity.
WRITABLE FILES: tmp/glm-plan.md
MUST ANSWER:
1. What issues were found in the plan? (brick selection, severity classification, agent assignments, missing KEY FILES, missing MUST ANSWER questions, dependencies, structural validation gaps, volume splitting omissions, CONVERGE justification gaps)
2. What changes were applied to produce the final plan? List each change with the specific issue it fixed.
3. Were any review findings rejected, and why?
4. Does the final plan have correct brick selection, severity classification, structural validation, complete KEY FILES for all agents, and valid MUST ANSWER questions?
