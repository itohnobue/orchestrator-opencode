=== TASK (s0-planner) ===
PROJECT: [fill in project path]
YOUR TASK: Research this project thoroughly and produce a custom Orchestration Workflow manifest. Explore the full codebase structure, read the agent INDEX at `.opencode/agents/INDEX.md`, classify the task on 5 axes (size, domain breadth, ambiguity, severity, change type), select appropriate bricks from the palette, and write the plan to `tmp/glm-plan.md`.
MUST ANSWER:
1. Domain breadth: how many distinct languages/frameworks does the codebase need? All execution uses executor — specialist identity comes from the research stage's FOCUS angles. Do not count audit lenses (test quality, security, documentation) — they apply to the same source code through different FOCUS angles, not separate domains.
2. What is the task classification on all 5 axes, with code-context justification for each? Include the Q1-Q5 severity checklist with YES/NO per question and one-line evidence.
2c. Write the CHANGE SPEC into the plan: Intent (1-2 lines: what this change achieves and why), Scope, Non-Goals (explicitly out of scope), Acceptance Criteria (AC-1..AC-n — observable, testable; EARS or Given/When/Then; one per promised deliverable), Definition of Done (the observable condition(s) that make the run complete), Assumptions/Open Questions. Calibrate depth to the size/severity classification — omit for tiny, ~1 page for small, full for medium/large; hard cap ~12 criteria. For each AC-n, name the stage/agent whose scope covers it.
3. Which bricks are selected and why? Which are skipped and why? State the CONVERGE CEILING for every DISCOVER and REVIEW stage (LOOP default / ONCE opt-in, operator-accepted residual risk — ceilings, trigger, and firing rules: AGENTS.md `#### Iterative Convergence`).
3b. For every DISCOVER and REVIEW stage (including CONVERGE iterations of these stages): state whether second opinions are required based on severity (MEDIUM+ → mandatory). For each stage that requires a second opinion, explicitly name the second agent in the stage's plan entry — the lead spawns whatever agents the plan lists, so it must be in the plan. Use a complementary FOCUS angle, never the primary's (see AGENTS.md Second Opinion Guidelines for default pairings). If no second opinion is required (severity < MEDIUM or LOW-only findings), justify briefly.
4. RESEARCH: List every external reference the codebase depends on — one row per named version; one research agent per row that passes the precision criterion (method, existence criteria, skip classes, examples: AGENTS.md `##### Brick Catalog` RESEARCH). Count from systematic grep, not ad-hoc reads; document every skip with a one-line reason in the plan.
5. For each agent you plan in the DISCOVER stage, state the agent's FILE SCOPES — module-level descriptions of what the agent should audit (e.g., "GPG core: core/GPGHandler.py, core/gpg_utils/*.py") with a rough Phase-1 LOC estimate. Do NOT list individual file paths — the volume-splitter resolves scopes to exact KEY FILES (size limits + split rules: AGENTS.md `##### Domain Splitting`). If the task spans 2+ domains: (a) classify each domain boundary using the Boundary Selection tiers (ALWAYS/DEFAULT/SKIP), (b) add intersection agents to DISCOVER for ALWAYS/DEFAULT boundaries — intersection agents MUST be in the first DISCOVER stage, never deferred to CONVERGE, (c) add cross-domain integration reviewers to REVIEW for ALWAYS/DEFAULT boundaries (same-specialist ALWAYS-tier boundaries also require both intersection and cross-domain reviewers), (d) document all boundary decisions in the manifest under "Boundary Analysis."
6. For each domain and sub-group agent, include 3-6 MUST ANSWER questions with expected evidence types (based on your Phase 1 code research). Include second opinion scope.
6b. For EVERY agent in DISCOVER/IMPLEMENT/REVIEW stages, assign the tier per the ONE general rule (see AGENTS.md — Tier rule): PLAIN or researched. Every researched agent must have routed report IDs from the Research Coverage Map — each as a (digest, full report) pair, in-scope only (precision rule); every s2 and intersection row must declare its complementary/boundary FOCUS angles.
7. What is the total agent estimate, broken down by stage?
8. Do the build and test commands work? Run them and report results. If the project's AGENTS.md or README says not to run them locally, state that and skip. Include the verified working commands (or reason for skipping) in the plan, plus a one-line Parallel-safety note for per-agent verification: whether targeted test files can run concurrently (standalone, no shared state) or per-agent verification is limited to compile/syntax checks (tests need a DB, ports, or shared build dirs).

STOP AFTER WRITING THE PLAN. When `tmp/glm-plan.md` is written, your work is complete. Do NOT execute any stage, spawn any agents, prepare any task files for Stage 1+, copy any files, or run any verification. The plan IS your deliverable. The volume-splitter and organizer handle the rest of Stage 0. The lead handles all execution from Stage 1 onward.

=== TASK (s0-volume) ===
PROJECT: [same project path]
PRIOR CONTEXT: The plan draft with FILE SCOPES is at `tmp/glm-plan.md`.
YOUR TASK: Read the plan at `tmp/glm-plan.md`. Resolve the planner's FILE SCOPES (skip already-resolved domains) to exact KEY FILES with `wc -l` counts, apply the mechanical split/merge rules, rewrite the plan in-place, and write `tmp/s0-volume-report.md` — follow the workflow in volume-splitter.md exactly (resolution + existence checks; volume audit table vs the 4K/12f baseline and 5.5K/18f narrow cap; split/merge rules incl. the thin-stub clause; preserve ALL planner content; correct the size classification mechanically).

Do NOT modify severity, boundaries, CONVERGE ceilings, agent selections, or scope overlap instructions. Do NOT spawn agents beyond your report.
WRITABLE FILES: tmp/glm-plan.md
MUST ANSWER:
1. What domains were split and why? List each split with exact pre-split and post-split LOC/file counts.
2. Were any merge-backs applied? Which splits were reversed and why?
3. Are all resolved file paths verified to exist on disk (test -f)? Any stale or missing paths?
4. What is the final volume audit summary — total agents, domains at baseline pass, close calls, splits applied?

=== TASK (s0-organize) ===
PROJECT: [same project path]
PRIOR CONTEXT: The plan with resolved KEY FILES and exact LOC is at `tmp/glm-plan.md`. The volume-splitter has already applied mechanical split/merge rules and built the volume audit table (see `tmp/s0-volume-report.md`).
YOUR TASK: Read the plan at `tmp/glm-plan.md`. Review it using your structural analytical framework (see agent-organizer.md):

1. Redistribute MUST ANSWER questions across split domains. When the volume-splitter created sub-agents by splitting a domain, the original MUST ANSWER questions were copied verbatim to all sub-agents. Assign each question to the sub-agent whose scope covers the relevant code. Write new scoped questions for split domains where the original questions don't cleanly map.

2. Workflow quality (native anti-patterns): Check for over-staffing, wrong agent assignments, redundant agents, vague delegations, ignored dependencies, and stale agent references. Verify every agent .md file referenced in the plan exists on disk.

3. Structural validation: Verify every DISCOVER/REVIEW stage has a corresponding VERIFY. Verify IMPLEMENT stages have a corresponding REVIEW. Verify MEDIUM+ severity tasks have second opinions in ALL DISCOVER and post-implementation REVIEW stages, including CONVERGE iterations (post-fix REVIEW inside FIX is primary-only — do NOT require seconds there). This includes intersection agents — at MEDIUM+ severity, each intersection agent gets a second opinion with a different specialist. Verify FIX stages include the BUILD-GATE and post-fix REVIEW. If CONVERGE iteration 2 is planned, verify no agent is reused from iteration 1 (cross-check exclusion list mechanically — do NOT trust the plan's claim). Verify cross-domain integration reviewers are present for all ALWAYS/DEFAULT boundaries. Verify domain breadth counts specialists, not packages. Volume splitting is already handled by the volume-splitter — do NOT duplicate or re-split. Spot-check the volume audit for obvious errors (e.g., a 6,000 LOC domain marked PASS) and flag if found. Verify sequential stages are genuinely dependent.

4. After review, apply all structural fixes directly to `tmp/glm-plan.md`. Volume adjustments are the splitter's domain — do NOT re-split or re-merge domains. Your report documents what was changed and why. Your output IS the final plan. This review runs on EVERY plan.

WRITABLE FILES: tmp/glm-plan.md
MUST ANSWER:
1. What issues were found in the plan? (brick selection, severity classification, agent assignments, missing MUST ANSWER questions, dependencies, structural validation gaps, CONVERGE justification gaps, stale agent references)
2. What MUST ANSWER question redistributions were applied for split domains?
3. What changes were applied to produce the final plan? List each change with the specific issue it fixed.
4. Were any review findings rejected, and why?
5. Does the final plan have correct brick selection, severity classification, structural validation, verified KEY FILES for all agents, valid MUST ANSWER questions, AND a valid Change Spec (Intent, Scope/Non-Goals, AC-n acceptance criteria, Definition of Done, Assumptions/Open Questions) with every criterion observable and routed to a covering stage/agent?
