=== DRAFT TASK (s0-planner) ===
PROJECT: [fill in project path]
YOUR TASK: Research this project thoroughly and produce a custom OpenCode workflow manifest. Explore the full codebase structure, read the agent INDEX at `.opencode/agents/INDEX.md`, classify the task on 5 axes (size, domain breadth, ambiguity, severity, change type), select appropriate bricks from the palette, and write the plan to `tmp/glm-plan.md`.
MUST ANSWER:
1. How many distinct domain areas does this project have?
2. What is the task classification on all 5 axes, with code-context justification for each?
3. Which bricks are selected and why? Which are skipped and why?
4. Which specialized agents from the INDEX fit each domain?
5. What is the total agent estimate?
6. Do the build and test commands work? Run them and report results. If the project's AGENTS.md or README says not to run them locally, state that and skip. Include the verified working commands (or reason for skipping) in the plan.

=== REVIEW TASK (s0-review-plan) ===
PROJECT: [same project path]
PRIOR CONTEXT: The draft plan is at `tmp/glm-plan.md`.
YOUR TASK: Read the draft plan. Review for correct brick selection, severity classification, agent assignment, verification placement, and convergence decisions. Challenge severity if it seems inflated or deflated. Check domain breadth (specialists needed, not packages). Apply all fixes and overwrite the plan with the final version. Do NOT redo full research — work from the existing draft.
WRITABLE FILES: tmp/glm-plan.md
MUST ANSWER:
1. Which changes were applied to the draft?
2. Does the final plan have correct brick selection and severity classification?
